Negative attitude and pressure does not always lift you up,keep the key low.  
**ALERT** : Not all these posts are necessary consistent with their literally written date.

## Local development

- `make doctor`
- `make build`
- `make serve`

## Content layout

- `/_posts`: published posts
- `/_drafts`: unpublished drafts
- root pages: long-lived entry pages such as `about`, `archive`, `themes`, `resources`
- posts with quote/link/code-heavy openings should define `summary` in front matter so homepage cards use that instead of blindly truncating the first paragraph

For future maintenance rules, see [`content-guide.md`](./content-guide.md).
